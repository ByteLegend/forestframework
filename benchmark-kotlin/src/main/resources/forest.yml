forest:
  http:
    port: 8080
  pg:
    connect:
      host: localhost
      port: 5432
      database: hello_world
      user: benchmarkdbuser
      password: benchmarkdbpass
      cachePreparedStatements: true
    pool:
      maxSize: 4
      maxWaitQueueSize: -1